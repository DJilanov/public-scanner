NODE_ENV=development
API_HOST=0.0.0.0
API_PORT=3001
WEB_PORT=5173
DATABASE_URL=postgres://public_scanner:public_scanner@localhost:5432/public_scanner
REDIS_URL=redis://localhost:6379

# To reuse the same staging Postgres location as ai-cv, open the tunnel first:
# ssh -fN -L 5454:localhost:5432 root@89.167.46.193
# Then point DATABASE_URL at the separate database:
# DATABASE_URL=postgresql://<user>:<password>@127.0.0.1:5454/public_scanner_dev
